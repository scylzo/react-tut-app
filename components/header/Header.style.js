import { StyleSheet } from "react-native";

export default style = StyleSheet.create({
  image: {
    width: 200,
  },
  text: {
    color: "#a1a1a1",
    marginTop: -20,
    marginBottom: 20,
  },
});
